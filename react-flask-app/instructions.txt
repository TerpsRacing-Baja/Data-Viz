Terminal 1:
cd to file/api
source venv/bin/activate (or just activate if windows)

//if on linux
yarn start-api

//this part does not work on windows yet a
 (cd venv\bin && set FLASK_APP = api.py && set FLASK_ENV=development && flask run if windows)



Terminal 2:
cd to file
yarn start (may need to npm install react-scripts --save)