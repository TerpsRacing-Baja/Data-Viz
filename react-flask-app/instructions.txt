Terminal 1:
cd to file/api

//if on linux
(may need to $ python3 -m venv venv)
source venv/bin/activate
yarn start-api

//this part does not work on windows yet
 (cd venv\bin && set FLASK_APP = api.py && set FLASK_ENV=development && flask run if windows)
 $ python -m venv venv
$ venv\Scripts\activate
 $ pip install flask python-dotenv

make sure to gitignore venv

Terminal 2:
cd to file
yarn start (may need to npm install react-scripts --save)
